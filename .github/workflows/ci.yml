name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn bootstrap
      - run: yarn build
      - run: yarn lint
      - run: yarn test-sauce
